# 采用 DevSecOps 方法实现 API 安全性

> 原文：<https://devops.com/why-traditional-approaches-to-api-security-dont-work/>

n 应用程序编程接口(API)是一种数据传输方法，它使应用程序内的服务能够与其他应用程序(或系统内的其他服务)对话。本质上，它使服务 A 能够以统一的方式与服务 B 对话。 公司 每天都从 API中获益，甚至不用去想它们。API 是 公司 从竞争对手那里获得市场份额的一种方式，并以新的令人兴奋的方式将应用程序货币化。 例如，e 非常时刻 某人s上至 他们 喜爱的社交媒体 平台或亚马逊上的店铺 s ，应用程序都是利用 API 来获取和 u 更新数据

发现和 s 获取 API 对于各行各业的 IT 团队来说是一场 永无止境的战斗。API， 开启新的攻击 表面目标 公司的 数据通过 vulne 攻击和暴露。在 Gartner 最近的 报告中， [API 安全:你需要什么 D o 来保护你的 API](https://www.gartner.com/en/documents/3956746/api-security-what-you-need-to-do-to-protect-your-apis)， 分析师 估计，到 2021 年， 90%的网络应用将会有更多的攻击面以的形式暴露出来 这个原来是准确的。

### 传统的 API 安全方法

传统上，API是由 e 企业 API 网关和内容 交付网络(CDNs)保护的 。虽然这些方法对冲了大多数的 和 【拒绝服务】 攻击，但一种更加细致入微和复杂的 攻击 可以 攻破保护。 传统的 API 安全方法充满了局限性。

公司 典型地 使用两种类型的[API](https://webinars.devops.com/api-security-privacy-and-governance-shift-left-devops-and-devsecops)–客户-app-面向 API也叫“南北 API”内部 API叫“东西 API。外部类型对所有 公共 i 互联网流量公开。 外部开发者可以向se面向客户的 API s 发出请求并接收响应，并利用应用的功能 和数据，同时绕过用户界面 (UI) 。 但是，内部 API 只对应用程序内部的 流量公开。例如， c art 服务在电子商务 应用的结账过程中，可能会通过 内部 API 调用ser 服务。

面向客户的 API倾向于多一点 的稳定、可见，并且针对的是开发者 的受众。这些南北走向的API 最大的挑战是确保第 层每天每一秒都是安全的。 这种开放且可访问的 API 是设计用于由发行商组织外的开发人员  使用。目标 是利用技术受众群体，增加组织的数据和服务的 使用率。偶尔 API 发布者鼓励开发者创建 在核心业务上扩展的应用。

虽然内部开发人员可能会调用南北API 用于他们的应用开发 ，但是更多的 通用 东西 服务 之间的 API 调用通常会驱动更多的增量特性开发和新的创新 。这些 API可能或者可能 而不是 是 可以被发布组织之外的开发者 访问。 但是，越来越多的东西向 API 是公共云中的原生 API，因此攻击面很容易暴露。此外，c对这些东西方 API的改动是不断进行的——以 a 公司的 DevOps 和 CI/CD 实践所允许的速度。 不幸的是，t h 是 变化速度会出错， 出错，而且安全疏漏的可能性更大。这些 API 和 的变化不太明显，这使得公司只有在黑客破坏了私人数据后才面临发现 漏洞的风险。

### API 的安全漏洞

数据通过所有 API 泄露，但 公司无法保护他们 不 知道的东西。API 在互联网上暴露的那一刻，就 为黑客提取数据创造了攻击载体。 如果世界上一些最大的公司在其 API和服务的整个代码库中都存在不安全性，那么 我们 可以打赌 其他每一家公司也会如此。

安全漏洞的 API 有 所以 占主导地位是因为 API 是这样一种 无处不在的传递信息的方法。数据泄露以多种形式出现 ，但它们几乎总是影响公司收入 和声誉。 几乎每天都会发现新的 API 违规和漏洞。 Capital One、微软、 T-Mobile、赛门铁克、麦当劳、Instagram、Salesforce.com、 和 Venmo 在过去几年里都经历过重大的 API 漏洞事件 。

[DevSecOps](https://devops.com/?s=DevSecOps)团队或 security 专家应持续做出基于 ris k 的决策，决定何时应缓解某些问题，以及 在CI/CD 管道中的何处应测试这些问题。但是，在关键场景中，需要主动的 防护来防止 安全事件的发生或升级。 IT 团队需要 使用工具 使 增加这层保护而不需要 人类参与。

传统的 API 安全方法虽然有助于减少攻击面，但速度太慢，跟不上不良分子不断演变的  战术。 只有具有明确的全栈视图的n 应用的 安全才能管理风险并强制执行 适当的 安全策略。没有 更好的 遥测 和 可观测性 ，每一个安全决策都将是无中生有，API 安全中的关键漏洞 都将被遗漏 。

### 持续不断地分析

S 安全流程和工具应不断分析云环境、API 网关、日志、web 和移动 应用程序以及源代码，以发现可能产生漏洞的变更。一旦ITisabletoidentify the changes totheirtheir可以确认认证、授权、加密、访问和 verificset

无需人工干预的主动防护是实现实时执行安全策略 并在攻击发生时进行防护的唯一方法。理想情况下， 一个安全解决方案应该 自动发现并检查 的 API 安全漏洞 、 ，同时人工分析情境风险并确定工作优先级。

有了适当的主动保护，when a security事件发生时， IT 部门将能够利用 关键信息 适当地识别受影响的系统组件以及导致 事件 的促成因素